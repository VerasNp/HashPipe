#include <stdlib.h>
#include <stdio.h>

#include "fileread.h"

//const char* _file_doread(ssize_t read, FILE* fp, char *line) {
//    size_t len = 0;
//    while ((read = getline(&line, &len, fp)) != -1) {
//        printf("Retrieved line of length %zu:\n", read);
//        printf("%s", line);
//    }
//}

